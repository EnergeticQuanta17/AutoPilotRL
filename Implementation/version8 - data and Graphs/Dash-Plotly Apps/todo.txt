1. Learning curve plotting

2. Convert all  the demo files to "study" related.

3. Plot Distributions of each of the Hyperparameter Distribution - overlapping

4. HeatMap

5. "bluered" color scale --> fig = px.scatter(
        df,
        x="sepal_width",
        y="sepal_length",
        color="sepal_length",
        color_continuous_scale="bluered",
    )

6. More complex dsitribions instead of Normal Distribution

7. Change all preprocess_df to this - Done from App21 onwards
        def preprocess_df(study, normalize=True):
        trials_df = study.trials_dataframe()

        for i in ['datetime_start', 'datetime_complete', 'state']:
            trials_df = trials_df.drop(i, axis=1)

        trials_df['duration'] = trials_df['duration'].dt.total_seconds()
        
        if(normalize):
            trials_df = trials_df.apply(lambda x: (x - x.min()) / (x.max() - x.min()))

        return trials_df

8. app21 - Find best fit Distribution and fit it